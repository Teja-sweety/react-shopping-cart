to create new class component in a file(with ES7 extension)
    type  rcc press tab   it gives

<!-- import React, { Component } from 'react'

export default class products extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
} -->